import Link from "next/link";
import { ROTULOS_ORIGEM_NEGOCIACAO, ROTULOS_STATUS_NEGOCIACAO, type Negociacao } from "@/core/negociacoes";
import type { Veiculo } from "@/core/veiculos";
export default function NegociacoesTabela({ negociacoes, veiculos }: { negociacoes: readonly Negociacao[]; veiculos: readonly Veiculo[] }) { return <div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead className="bg-slate-50 text-xs uppercase text-slate-500"><tr>{["Veículo", "Placa", "Interessado", "Telefone", "Origem", "Status", "Atualizado em", "Ação"].map((x) => <th className="px-5 py-3" key={x}>{x}</th>)}</tr></thead><tbody className="divide-y divide-slate-200">{negociacoes.map((n) => { const v = veiculos.find(({ id }) => id === n.veiculoId); return <tr key={n.id}><td className="px-5 py-4">{v ? [v.marca, v.modelo, v.versao].filter(Boolean).join(" ") : "Veículo não identificado"}</td><td className="px-5 py-4">{v?.placa ?? "—"}</td><td className="px-5 py-4 font-semibold">{n.interessadoNome}</td><td className="px-5 py-4">{n.interessadoTelefone}</td><td className="px-5 py-4">{ROTULOS_ORIGEM_NEGOCIACAO[n.origem]}</td><td className="px-5 py-4">{ROTULOS_STATUS_NEGOCIACAO[n.status]}</td><td className="px-5 py-4">{new Date(n.atualizadoEm).toLocaleString("pt-BR")}</td><td className="px-5 py-4"><Link href={`/negociacoes/${n.id}`} className="font-semibold">Ver</Link></td></tr>; })}</tbody></table></div>; }
