export default function NegociacoesPaginacao({ pagina, total, itensPorPagina, onPagina }: { pagina: number; total: number; itensPorPagina: number; onPagina: (p: number) => void }) { const paginas = Math.max(1, Math.ceil(total / itensPorPagina)); if (paginas <= 1) return null; return <div className="flex items-center justify-between border-t p-5 text-sm"><button disabled={pagina <= 1} onClick={() => onPagina(pagina - 1)}>Anterior</button><span>Página {pagina} de {paginas}</span><button disabled={pagina >= paginas} onClick={() => onPagina(pagina + 1)}>Próxima</button></div>; }
